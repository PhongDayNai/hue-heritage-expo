services:
  hue-heritage-expo:
    container_name: hue-heritage-expo
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "47080:3000"
    environment:
      - NODE_ENV=production
